# MCA Rail Registry (v0.1)

`x402` - HTTP 402 stablecoin-settled rails (e.g. Cloudflare pay-per-crawl class)

`ap2` - Agent Payments Protocol class mandates

`card` - Card-network agent programs

`ach` - Bank transfer rails

`other` - Any settlement mechanism not yet registered

Additions via PR (see GOVERNANCE.md).
